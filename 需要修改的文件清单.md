# MMaDA 图像 Token 改基因 Token 改造指南

## 一、需要修改的文件清单

### 1. 配置文件 (`configs/`)

| 文件 | 需修改的内容 |
|------|----------------|
| `mmada_demo.yaml` | 添加基因数据路径 |
| `mmada_pretraining_stage1_llada_instruct.yaml` | 添加 gene_vocab_size 等参数 |
| `mmada_pretraining_stage2_llada_instruct.yaml` | 添加细胞数据相关配置 |
| `mmada_pretraining_stage3_llada_instruct.yaml` | 添加细胞数据相关配置 |
| `mmada_pretraining_stage3_llada_instruct_512_cot.yaml` | 添加细胞数据相关配置 |
| `mmada_pretraining_stage4_llada_instruct.yaml` | 添加细胞数据相关配置 |

### 2. 模型定义 (`models/`)

| 文件 | 需修改的内容 |
|------|----------------|
| `configuration_llada.py` | 添加 gene_vocab_size, expression_bins 等参数 |
| `modeling_llada.py` | 添加基因嵌入层相关代码 |
| `modeling_mmada.py` | 主要改造点：处理基因 token 替代图像 VQ token |

### 3. 训练脚本 (`training/`)

| 文件 | 需要修改的内容 |
|------|----------------|
| `data.py` | 创建 CellwText 数据加载器，替换图像处理为基因处理 |
| `utils.py` | 添加基因相关的工具函数 |

### 4. 推理脚本 (`*.py`)

| 文件 | 需要修改的内容 |
|------|----------------|
| `inference_t2i.py` | 修改为基因生成推理 |
| `inference_mmu.py` | 修改为细胞-文本多模态理解 |

---

## 二、MMaDA 原始图像处理流程

### 2.1 VQ-VAE 编码 (models/modeling_magvitv2.py)

```python
class VQGANDecoder:
    # 将图像编码为离散的 VQ token
    # codebook_size: 8192
    # 输出: z_q (VQ token 索引), shape: [batch, height*width]
```

**需要改的地方**：
- 图像输入 → 基因表达矩阵输入
- VQ 编码器 → 需要创建基因表达量化器

### 2.2 模型输入处理 (models/modeling_mmada.py)

```python
# 原始 t2i_generate 方法
def t2i_generate(self, input_ids, ...):
    # input_ids 包含：
    # - lm 文本 tokens (前 seq_len 部分)
    # - vq_tokens (后 num_vq_tokens 部分，对应图像 VQ)
    # mask_token_id (126336) = "<|t2i|>"

    # 处理逻辑：
    # 1. 分离 lm_tokens 和 vq_tokens
    # 2. mask 掉除 vq_tokens
    # 3. 添加 <|t2i|> 标签
    # 4. 进行扩散生成
```

**需要改的地方**：
- `vq_tokens` → `gene_tokens` (基因 ID 序列)
- `num_vq_tokens` → `num_gene_tokens` (有表达的基因数量)
- `num_vq_tokens` → `gene_expression_tokens` (表达值序列)
- 添加 `<|g2t|>` 标签替换 `<|t2i|>`

### 2.3 数据加载 (training/data.py)

```python
# 原始 Text2ImageDataset
class Text2ImageDataset:
    # 加载图像数据，用 VQ 编码器编码为 vq_tokens

    # 需要的修改：
    # - 移除图像 VQ 编码器
    # - 直接加载基因表达矩阵
    # - 不进行 VQ 编码
```

---

## 三、基因 Token 设计

### 3.1 基因 Token 格式

| Token | 说明 | 示例 |
|-------|------|--------|
| `<|g2t|>` | 基因到文本开始 | 对应 `<|t2i|>` |
| `<|egv|>` | 表达值开始（可选） | 类似 VQ token |
| `gene_id` | 基因 ID | 40155 (cls) 或实际基因 ID |
| `gene_expression` | 基因表达值 | 1.23, 0.45, -2 (pad) |

### 3.2 输入序列格式

**MMaDA 原版（T2I）**:
```
input_ids = [
    lm_tokens...,      # 文本 token
    vq_tokens...,     # 图像 VQ token (1024个)
]
```

**CellwText 版本（G2T）**：
```
input_ids = [
    lm_tokens...,              # 文本 token (细胞元数据）
    gene_id_tokens...,         # 基因 ID token (1200个)
    gene_expression_tokens...,    # 基因表达值 token (可选)
]
```

---

## 四、需要修改的关键代码位置

### 4.1 modeling_mmada.py

```python
# 需要修改的部分：

# 1. 配置参数
class MMadaConfig(PretrainedConfig):
    vocab_size          # 需要新增：gene_vocab_size
    llm_vocab_size      # 原有
    codebook_size        # 原有
    num_vq_tokens      # 原有图像 VQ token 数
    num_gene_tokens     # 新增：有表达的基因 token 数
    gene_expression_bins  # 新增：表达值分箱数

# 2. t2i_generate 方法
def t2i_generate(self, input_ids, ...):
    # 原始逻辑：
    # vq_tokens = input_ids[:, -num_lm_vocab_size - 1:-1]  # 图像 VQ
    # mask_ratio = ...  # 计算 mask 比例

    # 修改后：
    # gene_tokens = input_ids[:, -num_lm_vocab_size - num_vq_tokens - 1:-1]  # 基因 ID
    # expression_tokens = input_ids[:, num_lm_vocab_size + num_vq_tokens:]  # 表达值（可选）
    # 添加新标签
```

### 4.2 data.py - 新建 CellwText 数据加载器

```python
# 新建 CellwTextDataset 类
class CellwTextDataset:
    def __init__(self, lmdb_path, gene_vocab_path, ...):
        # 加载 LMDB 数据
        # 加载基因映射词汇表

    def __getitem__(self, index):
        # 读取 LMDB 数据
        # 返回：{
        #     'input_ids': [lm_tokens, gene_tokens, expression_tokens],
        #     'attention_mask': [lm_mask, gene_mask, expression_mask],
        #     'labels': [lm_mask, gene_mask, expression_mask],
        # }
```

### 4.3 training/train_mmada.py - 主训练脚本

```python
# 需要修改的地方：
# - 数据加载器从 Text2ImageDataset 改为 CellwTextDataset
# - 适配模型输入格式
```

---

## 五、实现步骤

### 阶段 1: 模型适配
1. 修改 `MMadaConfig` 添加基因相关参数
2. 修改 `MMadaModelLM.t2i_generate()` 处理基因 token
3. 测试模型前向传播

### 阶段 2: 数据加载器
1. 创建 `CellwTextDataset` 类
2. 实现 LMDB 数据读取
3. 实现基因 token 转换逻辑
4. 测试数据加载

### 阶段 3: 训练脚本适配
1. 修改训练脚本使用新的数据加载器
2. 运行小规模测试

---

## 六、配置文件修改示例

### 新增配置项

```yaml
# configs/mmada_pretraining_stage1_llada_instruct.yaml (修改后)
model:
  mmada:
    pretrained_model_path: "LLaDA-8B-Instruct"
    new_vocab_size: 50121        # lm_vocab + gene_vocab_size
    codebook_size: 8192           # VQ codebook（保留用于其他任务）
    num_vq_tokens: 0              # 不需要，用基因替代
    num_gene_tokens: 1200         # 基因 token 数
    gene_expression_bins: 51          # 表达值分箱数

dataset:
  train_mmu_shards_path_or_url: "/path/to/cellwtext/lmdb"
  batch_size_mmu: 1               # 调试用小批量

training:
  batch_size_t2i: 1
  batch_size_lm: 1
  batch_size_mmu: 1
```

---

## 七、待解决的技术问题

### 1. 基因 VQ 编码器

MMaDA 使用 VQ-VAE 将图像编码为离散 token。对于基因表达：
- **方案 A**: 直接使用分箱后的基因 ID 作为 token
- **方案 B**: 单独创建一个 VQ 编码器将表达值量化

### 2. 注意力机制

原始 MMaDA: 图像 token 和文本 token 之间的交叉注意力
修改后: 可能需要基因 token 与文本 token 之间的特殊注意力

### 3. 训练损失函数

可能需要设计新的损失函数来处理基因表达值的预测
